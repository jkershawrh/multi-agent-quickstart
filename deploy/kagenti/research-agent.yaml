apiVersion: agent.kagenti.dev/v1alpha1
kind: Agent
metadata:
  name: research-agent
  namespace: multi-agent-quickstart
  labels:
    app.kubernetes.io/name: research-agent
    kagenti.io/type: agent
    kagenti.io/agent-protocol: a2a
  annotations:
    description: Research agent that investigates queries and gathers relevant information
spec:
  description: Investigate topics, gather data from multiple sources, and summarize findings for downstream analysis
  imageSource:
    image: quay.io/rh-ai-quickstart/multi-agent-quickstart:latest
  replicas: 1
  servicePorts:
    - name: http
      port: 8001
      protocol: TCP
      targetPort: 8001
  podTemplateSpec:
    spec:
      serviceAccountName: research-agent
      volumes:
        - name: tmp
          emptyDir: {}
      containers:
        - name: agent
          image: <will be replaced with imageSource.image above>
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8001
              protocol: TCP
          env:
            - name: AGENT_NAME
              value: "research"
            - name: AGENT_SKILLS
              value: "investigate,summarize"
            - name: AGENT_PORT
              value: "8001"
            - name: LOG_LEVEL
              value: "INFO"
            - name: MCP_SERVER_URL
              value: "http://mcp-server.multi-agent-quickstart.svc.cluster.local:8004"
            - name: XDG_CACHE_HOME
              value: "/tmp/.cache"
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          resources:
            limits:
              cpu: "1000m"
              memory: "512Mi"
            requests:
              cpu: "250m"
              memory: "256Mi"
